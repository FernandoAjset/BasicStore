<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ALCON Store</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/ProyectoPrimerParcial.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11.0.17/dist/sweetalert2.min.css" />
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3 navbar-auth">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Facturas" asp-action="Index">ALCON Store</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Facturas" asp-action="Index">Facturas</a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle text-dark" href="#" id="navbarDropdown" role="button"
                               data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                Módulos
                            </a>
                            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <a class="dropdown-item" asp-area="" asp-controller="Clientes" asp-action="Index">Clientes</a>
                                <a class="dropdown-item" asp-area="" asp-controller="Articulos" asp-action="Index">Artículos</a>
                            </div>
                        </li>
                    </ul>
                    <partial name="_LinksLogin" />
                </div>
            </div>  
        </nav>
    </header>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer text-muted">
        <div class="container">
            &copy; 2023 - ProyectoPrimerParcial - <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
        </div>
    </footer>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.0.17/dist/sweetalert2.all.min.js"></script>
    <script>
        // Función para ocultar el navbar cuando el usuario no está autenticado
        function ocultarNavbar() {
            $('.navbar-auth').hide();
        }

        // Función para mostrar el navbar cuando el usuario está autenticado
        function mostrarNavbar() {
            $('.navbar-auth').show();
        }

        // Lógica para verificar si el usuario está autenticado y ocultar/mostrar el navbar en consecuencia
        $(document).ready(function () {
            // Verificar si estamos en la página de inicio de sesión
            var isLoginPage = "@ViewContext.RouteData.Values["action"]" === "Login";

            // Llamar a las funciones para ocultar/mostrar el navbar
            if (!isLoginPage) {
                mostrarNavbar();
            } else {
                ocultarNavbar();
            }
        });
    </script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
